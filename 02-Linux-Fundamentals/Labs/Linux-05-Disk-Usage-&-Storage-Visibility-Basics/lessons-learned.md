# Lessons Learned

- `df -h` gives a quick overview of filesystem capacity
- `du -sh` is useful for comparing the size of directories
- `du -ah` with sorting helps identify large files and folders quickly
- `find` command be configured and used to locate files above a chosen size threshold
- Storage visibility is a small & important Linux skill for troubleshooting and investigation work