# Lessons Learned

- `tar` is used to bundle files and directories into one archive
- Adding `z` creates a gzip, a compressed archive
- Archived contents can be inspected without extracting them
- Extracted data should be verified after restoration
- Archiving and compression are practical Linux workflow skills for logs, configs, and backups